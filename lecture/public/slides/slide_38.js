Slides[ 38 ] = {
  init: function($el) {
  },

  steps: [
    function($el) { $el.appear(0); },
    function($el) { $el.appear(1); },
    function($el) { $el.appear(2); },
  ],

  run: function($el) {
  }
};
